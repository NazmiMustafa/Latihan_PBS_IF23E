# Environment variables declared in this file are NOT automatically loaded by Prisma.
# Please add `import "dotenv/config";` to your `prisma.config.ts` file, or use the Prisma CLI with Bun
# to load environment variables from .env files: https://pris.ly/prisma-config-env-vars.

# Prisma supports the native connection string format for PostgreSQL, MySQL, SQLite, SQL Server, MongoDB and CockroachDB.
# See the documentation for all the connection string options: https://pris.ly/d/connection-strings

# The following `prisma+postgres` URL is similar to the URL produced by running a local Prisma Postgres
# server with the `prisma dev` CLI command, when not choosing any non-default ports or settings. The API key, unlike the
# one found in a remote Prisma Postgres URL, does not contain any sensitive information.

DATABASE_URL="postgresql://postgres:postgres@localhost:5432/db_if23e_3001?schema=public"

# buat konstanta
PORT=3001
FAILED_SAVE='Data Kategori gagal disimpan'
SUCCESS_SAVE='Data Kategori Berhasil Di Simpan'
NOT_DATA='data tidak ditemukan !'
ALL_DATA='Berhasil Ini Semua Datanya !'
ONE_DATA='Berhasil Data Yang Anda Cari !'
NUMBER_ONLY='Parameter / slug ID harus Angka !'
FAILED_UPDATE='Data Kategori gagal diupdate'
SUCCESS_UPDATE='Berhasil Data Dirubah !'
SUCCESS_DELETE='Berhasil Data Di Hapus !'